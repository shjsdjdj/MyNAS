# Local-only by default. Set 0.0.0.0 only when LAN access is intentional.
MYNAS_HOST=127.0.0.1
MYNAS_ENV=development
MYNAS_VERSION=3.4.0
# Optional Windows launcher override. Prefer the project's .venv when available.
MYNAS_PYTHON=

# Public HTTPS deployment values. Keep origins explicit; never use "*".
MYNAS_PUBLIC_HOST=mynas.example.com
# Complete public origin only (no path). MYNAS_PUBLIC_URL remains supported for existing installs.
MYNAS_PUBLIC_BASE_URL=
MYNAS_PUBLIC_URL=
# Optional fixed, loopback-only cloudflared metrics endpoint for the read-only
# Public Access card. Leave empty to probe cloudflared's default ports 20241-20245.
MYNAS_TUNNEL_METRICS_URL=
MYNAS_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Trust proxy IP headers only when the local proxy addresses are explicitly known.
# Example for a local cloudflared connector: 127.0.0.1,::1
MYNAS_TRUSTED_PROXY_IPS=

# Backend security settings (set in PowerShell or Windows service environment).
MYNAS_ADMIN_USERNAME=admin
MYNAS_ADMIN_PASSWORD=replace-with-a-long-random-password
# Defaults to true (safe for production). Set to false ONLY for local HTTP development.
MYNAS_COOKIE_SECURE=false
MYNAS_JWT_EXPIRE_HOURS=24
MYNAS_JWT_LEEWAY_SECONDS=30
MYNAS_MAX_UPLOAD_BYTES=2147483648
MYNAS_MAX_REQUEST_BYTES=1048576
MYNAS_RATE_LIMIT_REQUESTS=300
MYNAS_RATE_LIMIT_WINDOW_SECONDS=60
